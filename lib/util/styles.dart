import 'package:flutter/material.dart';

class Styles {
  //
  //  --- Color ------------------------------------------------------------------
  //

  // [アイコン：女性アイコンの色]
  static Color femaleIconColor = Colors.pinkAccent;

  // [アイコン：男性アイコンの色]
  static Color maleIconColor = Colors.blueAccent;

  //
  //  --- TextStyle --------------------------------------------------------------
  //

  // [顧客データ表示：名前のスタイル]
  static var nameStyle = TextStyle(fontSize: 16, fontWeight: FontWeight.bold);

  // [顧客データ表示：よみがなのスタイル]
  static var nameReadingStyle = TextStyle(fontSize: 12, color: Colors.grey);
}
